module.exports = () => {

    var db = require("../database.js");

    var idUser;
    var username;
    var password;
    var firstname;
    var lastname;

    function createUser(username, password) {
        db.mysql_conn.query("");
    }
};